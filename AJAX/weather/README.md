# Weather AJAX Project

A simple weather checking web page built with HTML, vanilla CSS, and JavaScript.

## Dependencies
Loads weather data via local AJAX from `weather.json`.

## How to Install
No installation is required.

## How to Run
Serve this directory with a local static file server (e.g., Live Server) to allow AJAX requests to `weather.json` without CORS issues.
